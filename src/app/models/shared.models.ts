export enum ModalRoute {
  LOGIN = '[\'/account\',{ outlets: { modal: [\'login\'] } }]',
  FILTER = 'Filter',
  CATEGORY = 'Category',
  SEARCH = 'Search',
}
export type CarouselMode = 'vertical' | 'horizontal'
