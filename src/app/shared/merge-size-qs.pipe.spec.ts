import { MergeSizeQsPipe } from './merge-size-qs.pipe';

describe('MergeSizeQsPipe', () => {
  it('create an instance', () => {
    const pipe = new MergeSizeQsPipe();
    expect(pipe).toBeTruthy();
  });
});
